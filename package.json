{
  "name": "valorem-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "prepare:arbitrum-one": "mustache config/42161.json subgraph.template.yaml > subgraph.yaml && mustache config/42161.json src/utils/price.template.ts > src/utils/price.ts",
    "prepare:arbitrum-goerli": "mustache config/421613.json subgraph.template.yaml > subgraph.yaml && mustache config/421613.json src/utils/price.template.ts > src/utils/price.ts",
    "deploy:arbitrum-one": "pnpm prepare:arbitrum-one && pnpm build && graph deploy --product hosted-service valorem-labs-inc/valorem-v1-arbitrum",
    "deploy:arbitrum-goerli": "pnpm prepare:arbitrum-goerli && pnpm build && graph deploy --product hosted-service valorem-labs-inc/valorem-v1-arbitrum-goerli",
    "deploy:all": "pnpm deploy:arbitrum-goerli && pnpm deploy:arbitrum-one"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.46.1",
    "@graphprotocol/graph-ts": "^0.29.3"
  },
  "devDependencies": {
    "mustache": "^4.2.0"
  }
}
